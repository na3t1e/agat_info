{% extends 'baseAdmin.html.twig' %}
{% form_theme contactEditForm 'bootstrap_5_layout.html.twig' %}
{% block title %}Администратор - Контакты {% endblock %}

{% block body %}
    {{ form_start(contactEditForm) }}
    <div class="row">

    </div>
    <div class="col-sm-12 col-md-6">
        {{ form_row(contactEditForm.number1) }}
        {{ form_row(contactEditForm.number2) }}
        {{ form_row(contactEditForm.email) }}
        {{ form_row(contactEditForm.tg) }}
        {{ form_row(contactEditForm.wa) }}
        {{ form_row(contactEditForm.workTime) }}
        {{ form_row(contactEditForm.address) }}
    </div>
    {{ form_row(contactEditForm.save) }}


    </div>
    {{ form_end(contactEditForm) }}

    <script>
        $("#contact_number2").mask("+7(999) 999-99-99");
        $("#contact_number1").mask("+7(999) 999-99-99");
    </script>

{% endblock %}
